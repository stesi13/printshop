# Copy to .env and fill in values.
# Local dev: point at the docker-postgres helper (see docs/catalog-model.md).
# Production: Neon connection string (provisioned separately).
DATABASE_URL="postgresql://printshop:printshop@localhost:5544/printshop?schema=public"
